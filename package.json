{
  "name": "callcap-desktop",
  "version": "0.1.5",
  "description": "Callcap desktop recorder",
  "main": "src/main.cjs",
  "author": "Callcap",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build:mac": "electron-builder --mac",
    "build:win": "electron-builder --win",
    "build:linux": "electron-builder --linux",
    "publish": "electron-builder --publish always"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "build": {
    "appId": "app.callcap.recorder",
    "productName": "Callcap",
    "asar": true,
    "directories": {
      "buildResources": "build"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "protocols": [
      {
        "name": "Callcap",
        "schemes": ["callcap"]
      }
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "target": [{ "target": "zip", "arch": ["x64", "arm64"] }],
      "artifactName": "Callcap.${ext}",
      "icon": "build/icon.png",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "notarize": true,
      "extendInfo": {
        "NSMicrophoneUsageDescription": "Callcap records your call audio to transcribe and summarize your meetings."
      }
    },
    "win": {
      "target": [{ "target": "nsis", "arch": ["x64"] }],
      "artifactName": "Callcap-Setup.${ext}",
      "icon": "build/icon.png",
      "signAndEditExecutable": false
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true
    },
    "linux": {
      "target": [{ "target": "AppImage", "arch": ["x64"] }],
      "category": "AudioVideo",
      "artifactName": "Callcap.${ext}",
      "icon": "build/icon.png"
    },
    "publish": {
      "provider": "github",
      "owner": "Joel-bre",
      "repo": "callcap-desktop",
      "releaseType": "release"
    }
  }
}
